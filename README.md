# How to run this project

## Step 1:

- Install npm (node package manager)
  ```
  npm install
  ```

## Step 2:

- Install vite
  ```
  npm install vite
  ```

## Step 3:

- Run the project
  ```
  npm run dev
  ```
